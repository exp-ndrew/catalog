<%= form_for @photo, :url => item_photos_path(@item), :html => { :multipart => true } do |form| %>
        <%= form.file_field :image %>
        <%= form.submit "Add Image", :class => "btn btn-default" %>
<% end %>
