<h2>Add a Photo to <%= @item.name %></h2>

<div id="errors">
  <%= render '/layouts/errors', :object => @photo %>
</div>

<%= form_for [@item, @photo], :html => { :multipart => true } do |form| %>
  <%= form.file_field :item_image %>
  <%= form.submit :class => "btn btn-primary" %>
<% end %>
