


<h2>Items</h2>
  <ul>
    <% @items.each do |item| %>
      <li><%= link_to "#{item.name}", "/items/#{item.id}" %></li>
    <% end %>
  </ul>
  <%= link_to "Add Item", new_item_path, :class => "btn btn-primary" %>
</div>
